# Welcome to tmr

This is a throwaway note in the `sandbox/` playground — nothing you do
here can affect your real notes elsewhere.

## Try it out

- [ ] Move the cursor to this line (arrow keys) and press `Space` to
      check it off
- [ ] Press `Enter` to start editing this file, type something, then
      `Ctrl+S` to save
- [ ] Press `Esc` to go back, then `Tab` to switch focus to the Files
      pane, then `Ctrl+N` to create a new file
- [x] Read `showcase.md` next for the full Markdown feature tour

## Notes

Anything you type here is safe to lose — this file is tracked in git, so
`git checkout -- sandbox/welcome.md` restores it if you want a clean copy.
